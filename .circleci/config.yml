jobs:
  test-app:
    docker:
      - image: paszser/comparerapp:latest
    steps:
      - checkout
      - run:
          name: "Task para app test"
          command: "rake test"

workflows:
  version: 2
  test-app-workflow:
    jobs:
      - test-app 